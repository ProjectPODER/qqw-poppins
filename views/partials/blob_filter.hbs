{{#if filters }}
  <p class="search-terms-text">
    Búsqueda para:
    {{#each filters }}
      {{#ifCond this.hidden '!=' true }}
        <span class="blob" data-field="{{this.htmlFieldName}}">
          {{this.fieldLabel}}: {{this.value}}
          <i class="fa fa-times delete-blob-filter" aria-hidden="true"><b>x</b></i>
        </span>
      {{/ifCond}}
    {{/each}}
  </p>
{{/if}}
